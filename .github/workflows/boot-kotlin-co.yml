name: boot-kotlin-co

on:
  push:
    paths:
      - "boot-kotlin-co/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-kotlin-co/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "25"
          cache: "maven"
      - name: Start up PostgreSQL in Docker
        run: |
          docker compose up -d postgres
          sleep 10
          docker ps -a
      - name: Build with Maven
        run: |
          mvn clean install --file boot-kotlin-co/pom.xml
