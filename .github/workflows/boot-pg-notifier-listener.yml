name: boot-pg-notifier-listener

on:
  push:
    paths:
      - "boot-pg-notifier-listener/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-pg-notifier-listener/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "25"
          cache: "maven"
      - name: Build with Maven
        run: |
          mvn clean install --file boot-pg-notifier-listener/pom.xml