name: kotlin-co

on:
  push:
    paths:
      - "kotlin-co/**"
    branches:
      - master
  pull_request:
    paths:
      - "kotlin-co/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "21"
          cache: "maven"
      - name: Start up PostgreSQL in Docker
        run: |
          docker compose up -d postgres
          sleep 10
          docker ps -a
      - name: Build with Maven
        run: |
          mvn clean install --file kotlin-co/pom.xml
