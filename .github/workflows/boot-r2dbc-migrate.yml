name: boot-r2dbc-migrate

on:
  push:
    paths:
      - "boot-r2dbc-migrate/**"
    branches:
      - master
  pull_request:
    paths:
      - "boot-r2dbc-migrate/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "25"
          cache: "maven"
      - name: Build with Maven
        run: |
          mvn clean install --file boot-r2dbc-migrate/pom.xml